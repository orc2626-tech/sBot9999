# Dashboard — copy to .env to connect to backend (optional)

# Backend API URL (default: http://127.0.0.1:3000)
VITE_API_URL=http://127.0.0.1:3001

# Admin token — must match backend AURORA_ADMIN_TOKEN for /state and control endpoints
VITE_ADMIN_TOKEN=your-secret-admin-token-change-me
