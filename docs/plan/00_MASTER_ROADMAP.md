# 🗺️ AURORA SPOT NEXUS — الخطة الرئيسية الكاملة
## Version: 2.0 FINAL | 2026-02-07
## ⚠️ اقرأ هذا الملف أولاً قبل أي شيء آخر

---

## ⛔ القواعد المطلقة

```
╔═══════════════════════════════════════════════════════════════╗
║           ⚠️ نظام تداول حقيقي — أموال حقيقية ⚠️             ║
╠═══════════════════════════════════════════════════════════════╣
║ 1. اقرأ main.rs + الملفات المتأثرة بالكامل قبل أي تعديل   ║
║ 2. أنشئ checklist قبل أي تغيير                               ║
║ 3. لا تفترض — تحقق من الكود الفعلي                          ║
║ 4. تحقق ثلاث مرات قبل commit                                ║
║ 5. اطلب موافقة سعيد على كل خطوة                             ║
║ 6. كود إنتاج فقط — لا أمثلة تعليمية                         ║
║ 7. error handling + logging + edge cases في كل سطر           ║
║ 8. NaN/Infinity → block trade فوراً                          ║
║ 9. WS disconnect → pause trading                             ║
║ 10. لا unwrap() في production — استخدم ? أو match            ║
║ 11. كل API: timeout + retry + rate limit                     ║
║ 12. كل WS: reconnect + heartbeat                            ║
║ 13. صفر regressions — حافظ على كل ما يعمل                  ║
║ 14. إذا شككت → توقف واسأل سعيد                             ║
╚═══════════════════════════════════════════════════════════════╝
```

---

## 📊 حالة المشروع الحالية

```
Backend: Rust (tokio async) — 1,203 سطر — 16 ملف
Dashboard: React + TypeScript + Vite + Tailwind — 1,233 سطر — 30 ملف

✅ ما يعمل: API (15+ endpoints), Auth, Config, WS User Stream,
   Control Panel, GlobalTruthHeader, atomic state versioning,
   runtime_config (atomic save), parking_lot::RwLock

🔴 ما لا يعمل:
   strategy.rs = فارغ (Vec::new)
   execution.rs = فارغ (6 أسطر)
   risk.rs = struct بلا منطق
   position_engine.rs = struct فقط
   لا market data, لا indicators, لا order placement
   90% Dashboard = hardcoded data
```

---

## 🗓️ خطة التنفيذ — 6 ملفات بالترتيب

| الملف | المرحلة | الأسبوع | ما يُنتج |
|-------|---------|---------|----------|
| `01_PHASE_0_FOUNDATIONS.md` | الأساسيات | 1-2 | يرى السوق + يتداول + يحمي نفسه |
| `02_PHASE_1_SUPERWEAPONS.md` | الأسلحة الخارقة | 3-4 | يعرف نوع السوق + يرفض الضوضاء |
| `03_PHASE_2_FUTURES_INTEL.md` | بيانات Futures | 5 | يرى 70% من السوق المخفي |
| `04_PHASE_3_SELF_LEARNING.md` | ذكاء ذاتي | 6 | يتعلم + يتحسن تلقائياً |
| `05_PHASE_4_DASHBOARD.md` | الداشبورد | متوازي | داشبورد حي بمستوى Hedge Fund |
| `06_PHASE_5_ASSEMBLY.md` | التجميع | 7 | strategy.rs = عقل البوت الكامل |

**القاعدة**: لا تبدأ مرحلة حتى تعمل السابقة 100%.
**الاستثناء**: Phase 4 (Dashboard) يمكن أن تبدأ متوازية.

---

## 🔌 تبعيات المراحل

```
Phase 0 → Phase 1 (Regime يحتاج شموع، VPIN يحتاج trades)
Phase 0 → Phase 2 (Futures يحتاج client infrastructure)
Phase 1 → Phase 3 (Learning يحتاج weighted scores)
Phase 0+1+2+3 → Phase 5 (Assembly يجمع الكل)
Phase 0+1+2+3 → Phase 4 (Dashboard يعرض الكل)
```

## 📏 معايير الجودة

**Rust**: `cargo build` + `cargo clippy --all-targets` + `cargo test` = صفر أخطاء
**كل function عامة**: doc comment + error handling + logging
**كل WebSocket**: reconnect loop + exponential backoff
**كل API call**: timeout (10s) + retry (3x) + rate limit check

---

*ابدأ بالملف التالي: 01_PHASE_0_FOUNDATIONS.md*
